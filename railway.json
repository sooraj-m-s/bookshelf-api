{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "python -m pip install --upgrade pip && pip install -r requirements.txt"
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "python bookshelf_api/manage.py collectstatic --noinput && python bookshelf_api/manage.py migrate && python bookshelf_api/manage.py runserver 0.0.0.0:$PORT"
  }
}